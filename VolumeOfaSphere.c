//volume of a sphere with a given radius=5
#include<stdio.h>
int main()
{
    float radius = 5;
    float volume = 4 * 3.14 * radius * radius * radius / 3;
    printf(" The volume of sphere is:%f",volume);
    return 0;
}